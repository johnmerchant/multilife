apiVersion: apps/v1
kind: Deployment
metadata:
  name: multilife
  labels:
    app: multilife
spec:
  selector:
    matchLabels:
      app: multilife
  template:
    metadata:
      labels:
        app: multilife
    spec:
      volumes:
      - name: www-data
        emptyDir: {}
      containers:
      - name: multilife-client
        image: nginx:alpine
        volumeMounts:
          - name: www-data
            mountPath: /usr/share/nginx/html
        ports:
        - containerPort: 80
          protocol: TCP
      - name: multilife-server
        image: jmercha/multilife
        command: ["/bin/sh"]
        args: ["-c", "cp -R /app/dist/client/* /www-data/ && /app/entrypoint.sh"]
        volumeMounts:
        - name: www-data
          mountPath: /www-data
        ports:
        - containerPort: 5000
          protocol: TCP
        - containerPort: 31337
          protocol: UDP